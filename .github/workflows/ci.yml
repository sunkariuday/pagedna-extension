name: PageDNA CI
on: [push, pull_request]
jobs: {test: {runs-on: ubuntu-latest, steps: [{uses: actions/checkout@v4}, {run: npm test}, {run: 'find src tests -name "*.js" -print0 | xargs -0 -n1 node --check'}]}}
