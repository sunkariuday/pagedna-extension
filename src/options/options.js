async function send(type, payload = {}) { return chrome.runtime.sendMessage({ type, ...payload }); }
async function init() { const r = await send('PD_GET_SETTINGS'); const s = r.settings; for (const id of ['paused','mediumNotifications','highNotifications','criticalNotifications']) document.getElementById(id).checked = Boolean(s[id]); document.getElementById('medium').value = s.thresholds.medium; document.getElementById('high').value = s.thresholds.high; document.getElementById('critical').value = s.thresholds.critical; }
document.getElementById('save').onclick = async () => { const settings = { paused: document.getElementById('paused').checked, mediumNotifications: document.getElementById('mediumNotifications').checked, highNotifications: document.getElementById('highNotifications').checked, criticalNotifications: document.getElementById('criticalNotifications').checked, thresholds: { medium: Number(document.getElementById('medium').value), high: Number(document.getElementById('high').value), critical: Number(document.getElementById('critical').value) } }; await send('PD_SAVE_SETTINGS', { settings }); document.getElementById('saved').textContent = 'Saved'; setTimeout(() => document.getElementById('saved').textContent = '', 1800); };
init();
